apiVersion: apps/v1
kind: Deployment
metadata:
  name: campagne-service
  namespace: senprix
  labels:
    app: campagne-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: campagne-service
  template:
    metadata:
      labels:
        app: campagne-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8083"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: campagne-service
          image: senprix/campagne-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          env:
            - name: DB_URL
              value: "jdbc:postgresql://postgres:5432/campagne_db"
            - name: KEYCLOAK_JWK_URI
              value: "http://keycloak:8080/realms/senprix/protocol/openid-connect/certs"
            - name: USER_SERVICE_URL
              value: "http://user-service:8081"
            - name: PRODUIT_SERVICE_URL
              value: "http://produit-service:8082"
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            failureThreshold: 60
            periodSeconds: 5
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: campagne-service
  namespace: senprix
spec:
  selector:
    app: campagne-service
  ports:
    - port: 8083
      targetPort: 8083
